# Project status — 2026-09-22 (day 2, morning)

## Done
- Contracts and scaffold: `models.py`, `registry.py`, `config.py`, base ABCs, `sql/001_schema.sql`, docker-compose (pgvector on :5433), CI, pre-commit, `CLAUDE.md` regulations.
- Ingestion: clone of SOP-MINI/sop-site + Kozlowski pdfs -> `data/raw/<course>/<lab>/{lab.xml,src,slides,extra,summary.md,summary.pdf,manifest.json}`; 231 documents, 12 labs; Kozlowski = `support=background`, sop-site = `primary`. `docs/corpus.md`.
- Retrieval: chunkers `fixed | hierarchical | semantic`; embedders `bge_m3 | bge_m3_hf | fake`; searchers `lexical | lexical_idf | dense | hybrid_rrf | hybrid_rrf_idf | graph_walk`; rags `vector | graph`. Graph: 1131 nodes / 5130 edges in Postgres + networkx.
- Generation + agent: `anthropic` (claude-opus-5, adaptive thinking) and `fake` providers; prompts as `.md`; LangGraph ReAct agent with tools generate_lab / explain_topic / visualize_concept; CLI `agent`, `chat`.
- Eval: 61 gold queries, metrics hit/recall/section_recall/MRR/nDCG/latency/context stats, `rag-lab eval --matrix`, LLM judge stub. Results in `docs/eval.md` and `results/`.
  Winner: vector / dense / hierarchical / bge_m3 (nDCG@8 0.625). IDF lexical +0.15. Graph best at hops=1 (0.563). Defaults updated: SEARCHER=hybrid_rrf_idf, GRAPH_HOPS=1.
- Web: FastAPI `src/rag_lab_generator/api/` + React UI `web/` (Chat, Corpus, Retrieval Lab, Graph, Eval, Status).
- QA: `docs/qa-report.md` (backend), `docs/qa-web-report.md` (web, may still be in progress), suites in `tests/unit/qa`, `tests/integration/e2e`, `tests/unit/api_qa`, `web/tests`.
- Deployed to Vercel: https://rag-lab-generator.vercel.app (public). Neon Postgres with the full corpus. See README "Deployment (Vercel)" and the memory note.
- 2026-09-22: zip attachments unpacked. `layout.build_lab` extracts the text sources of every `src/*.zip` into `src/<zip-stem>/` (binaries and non-code files skipped), registers them in `lab.xml <sources>` and `manifest.json`, and the graph extractor links each task to the code of its attachments (`RELATED_TO`). Result: 10 files (sop1l4e2, sop1l4e3, sop2l6e3), 12 hierarchical CODE chunks, 10 new code_file nodes; `sop2l8e3-4.zip` holds only compiled `server`/`client`, nothing to unpack. Local db: chunks hierarchical 5581, bge_m3 and fake complete for hierarchical (0 chunks without vector), nodes 1141 / edges 5140. Tests: `tests/unit/ingestion/test_layout.py`, `test_extractor.py::test_task_is_related_to_the_code_unpacked_from_its_attachment`.

## In flight when the day ended (check before continuing)
1. DONE 2026-09-22. The re-chunk had finished but bge_m3 covered only 4128 of the 5569 hierarchical chunks (the 5746 total included the 1618 `fixed` chunks); `rag-lab index --embedder bge_m3 --strategy hierarchical` embedded the missing 1453 (1441 old + 12 new). Re-run `rag-lab eval --config vector/dense/hierarchical/bge_m3` to confirm the numbers in `docs/eval.md` were not taken on the partial index.
2. Agent C was adding a code-kind penalty + lecture reachability to `graph_walk.py` and re-running `rag-lab eval --config graph/dense/hierarchical/bge_m3`.
3. Web QA agent (Playwright) may have left `docs/qa-web-report.md` and `web/tests/e2e/`; `tests/unit/api_qa/test_api_contract.py` needs `ruff format`.
4. Neon (production db) still holds the OLD chunk set (no unpacked code, task chunks without `task:<id>` section ids). Re-upload: `pg_dump ... --exclude-table-data=embeddings` + bge_m3 embeddings TSV (see README deployment section), then `vercel deploy --prod --yes` so the function ships the unpacked `data/raw/**/src/<zip-stem>/` files too.

## CI data pipeline (2026-09-22, agreed with the user)
- `.github/workflows/rebuild-db.yml`: chunk (hierarchical + fixed with the fake splitter, semantic grouped by bge_m3_hf) -> index bge_m3_hf for all three strategies -> graph-build -> three evals -> job summary + artifact. Writes straight into production Neon (`chunk --replace` leaves a few-minute window without chunks; Neon-branch + restore is the follow-up if that hurts). Corpus lives in git: `.gitignore` now keeps the text of `data/raw` (306 files, 3 MB), pdfs / `slides/` / `data/external` stay ignored; `extra/` sidecars are kept because `load_documents` reads them.
- `.github/workflows/deploy.yml`: PR -> Vercel preview + PR comment, push to main -> production, via VERCEL_TOKEN (Hobby team cannot add members and rejects foreign commit authors on the Git integration).
- Fixes needed for the pipeline to work: `Embedder.vector_space` (bge_m3_hf reads/writes the bge_m3 rows) in searchers, `rag-lab index` and `eval/runner.py::Prerequisites` (it skipped `.../bge_m3_hf` configs as "no embeddings").
- Secrets to add on GitHub: VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID, NEON_DATABASE_URL_UNPOOLED, HF_API_TOKEN. First run to watch: HF free-tier credits (about 750 requests per rebuild); fallback is computing bge-m3 on the runner with the `embed-local` extra.
- Not yet done: nothing is committed or pushed (git is read-only for the agent); the user commits the workflows, `.gitignore`, `data/raw` text and today's code.

## Next steps (agreed / suggested)
- Zip attachments: done (see Done). Only `hierarchical` was re-chunked; `fixed` and `semantic` do not contain the unpacked code until re-chunked with `--replace`.
- Chunk quality (2026-09-22, user report: graph nodes full of heading-only / broken-code chunks). Cause: `hierarchical` sends sections above chunk_size to `SemanticChunker.split_text`, which was run with the `fake` embedder (random vectors -> a new group at nearly every paragraph) and split paragraphs on blank lines *inside* fenced code. Fix in `chunking/semantic.py`: `split_paragraphs` keeps a fenced block whole; `_merge_small` folds pieces below `MIN_PIECE_RATIO` (0.25) * chunk_size into their predecessor and drops the repeated overlap paragraph. Corpus effect: hierarchical 5581 -> 3107 chunks (avg 512 -> 834 chars), tutorial/task chunks under 150 chars 160 -> 12; `sop2/l5#stage-3` went from 17 fragments to 2 pieces with the code block intact.
  Eval effect at chunk_size 1600 (k=8, old -> new): vector/dense hit 0.967 -> 0.918, sect 0.744 -> 0.733, MRR 0.841 -> 0.769, nDCG 0.625 -> 0.556, context chars 6932 -> 9349; vector/hybrid_rrf_idf hit 0.951 -> 0.951, sect 0.756 -> 0.733, MRR 0.799 -> 0.815, nDCG 0.595 -> 0.583; graph/dense hops=1 hit 0.951 -> 0.885, sect 0.500 -> 0.616, nDCG 0.563 -> 0.528. Reading: the old chunk-level numbers were inflated by fragmentation (`the-alarm-semaphores` was 26 fragments, each a separate "relevant" hit); section recall is flat for vector and up for graph. Three queries lost their top-8 dense hit: l4_semaphore, l0_program_arguments, l7_unix_socket (the l7 target chunk is byte-identical, it was outranked by now-whole neighbouring sections). Control run at CHUNK_SIZE=1000: dense nDCG 0.611 / hit 0.951 / sect 0.686, hybrid 0.600 / 0.967 / 0.686, graph h=1 0.617 / 0.918 / 0.616, context 7.6k chars -> **CHUNK_SIZE default changed to 1000** (config.py, .env, .env.template); local db now holds the 1000-char set (hierarchical 3719, bge_m3 + fake complete, graph 1141/5140). Comparison table in `docs/eval.md` ("2026-09-22 re-chunk"). The 20-row matrix and hop sweep in `docs/eval.md` still describe the OLD chunk set: re-run `rag-lab eval --matrix -k 8` and the hop sweep before the numbers go into the thesis. Neon still has the old fragmented chunks until re-uploaded.
- HF_API_TOKEN: DONE 2026-09-22, new fine-grained token added to Vercel production and deployed. Found and fixed a second prod bug on the way: `bge_m3_hf` looked up vectors under its own name while Neon stores them as `bge_m3`, so dense returned 0 chunks and hybrid silently degraded to lexical. `Embedder.vector_space` (base.py) now maps `bge_m3_hf` -> `bge_m3`; dense/hybrid searchers and `rag-lab index` use it. Neon holds bge_m3 vectors for `hierarchical` only, so dense on `fixed`/`semantic` is empty on prod by design.
- ANTHROPIC_API_KEY: add to `.env` and Vercel (`LLM_PROVIDER=anthropic`) to get real generation; `summarize` command regenerates summaries with the real model.
- Eval follow-ups: index `semantic` chunks with bge_m3 and add to the matrix; k sweep (4/8/12); `--course-filter` run; consider filtering `summary` chunks out of generation context (they dominate dense results for generation queries).
- Retrieval latency: add connection pooling (psycopg_pool) — every store call opens a connection.
- Thesis writing: numbers and reading in `docs/eval.md`; graph export in `results/graph.graphml`.

<?xml version='1.0' encoding='UTF-8'?>
<lab id="sop2/l5_5" course="sop2" number="5.5" slug="l5_5_posix_queues" lang="en" source_url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/">
  <title>POSIX Queues (extra topic)</title>
  <topics>
    <topic>mq_overview</topic>
    <topic>mq_open</topic>
    <topic>O_RDONLY</topic>
    <topic>O_WRONLY</topic>
    <topic>O_RDWR</topic>
    <topic>O_CREAT</topic>
    <topic>O_NONBLOCK</topic>
    <topic>NULL</topic>
    <topic>O_EXCL</topic>
    <topic>mqd_t</topic>
    <topic>mq_close</topic>
    <topic>mq_unlink</topic>
    <topic>mq_getattr</topic>
    <topic>mq_setattr</topic>
    <topic>mq_send</topic>
    <topic>mq_receive</topic>
    <topic>MQ_PRIO_MAX</topic>
    <topic>EAGAIN</topic>
    <topic>mq_timedsend</topic>
    <topic>mq_timedreceive</topic>
    <topic>ETIMEDOUT</topic>
    <topic>CLOCK_REALTIME</topic>
    <topic>clock_getres</topic>
    <topic>mq_timed</topic>
    <topic>mq_</topic>
    <topic>mq_notify</topic>
    <topic>sigevent</topic>
    <topic>SIGEV_SIGNAL</topic>
    <topic>SIGRTMIN</topic>
    <topic>sigaction</topic>
    <topic>SA_SIGINFO</topic>
    <topic>sethandler</topic>
    <topic>mq_handler</topic>
    <topic>SIGEV_THREAD</topic>
    <topic>EINVAL</topic>
    <topic>EBUSY</topic>
    <topic>kill</topic>
    <topic>TEMP_FAILURE_RETRY</topic>
    <topic>waitpid</topic>
    <topic>SIGCHLD</topic>
    <topic>CHILD_COUNT</topic>
    <topic>ROUNDS</topic>
    <topic>child_function</topic>
    <topic>handle_messages</topic>
    <topic>register_notification</topic>
    <topic>exit</topic>
    <topic>spawn_child</topic>
  </topics>
  <tutorial>
    <section id="tutorial-5-5-posix-message-queues" title="Tutorial 5.5 - POSIX Message Queues" level="1" order="0" parent="">
      <text><![CDATA[# Tutorial 5.5 - POSIX Message Queues


> This tutorial covers topics not checked during graded laboratories.
> Still, message queues are useful, so we leave it here and encourage everyone to read it.]]></text>
    </section>
    <section id="posix-queue-operations" title="POSIX Queue Operations" level="2" order="1" parent="tutorial-5-5-posix-message-queues">
      <text><![CDATA[## POSIX Queue Operations

The topic of message queues is noticeably less extensive than that of pipes/FIFOs, and this tutorial is also shorter.  
POSIX message queues don’t suffer from issues with simultaneous reading and writing, guarantee atomic writes even for large messages, provide distinct message boundaries, and support priorities. These features make the topic relatively simple, with potential difficulties mostly limited to handling queue event notifications.

The main focus of this session is POSIX message queues, but the tasks will also involve elements practiced earlier, such as threads, processes, and signals.

The manual `man 7 mq_overview` contains a description of how queues work. The discussed functions are defined in the `<mqueue.h>` header file, and the interface is designed in a way that allows queues to be used similarly to file operations — it's worth noting the parallels in how each function is defined and used.

POSIX queues are **priority-based**, meaning the order of messages is determined first by priority and then by send order.]]></text>
    </section>
    <section id="creating-opening-and-removing-queues" title="Creating, Opening, and Removing Queues" level="3" order="2" parent="posix-queue-operations">
      <text><![CDATA[### Creating, Opening, and Removing Queues

Queues are identified by a name (defined by the programmer) `/<name>`, e.g., `/queue`.  
To create and open a queue, use the `mq_open` function (`man 3p mq_open`):

```
mqd_t mq_open(const char *name, int oflag, ...);
```

- `name` is the name of the queue,  
- `oflag` is a bitmask defining the access mode. Available options include:
  - `O_RDONLY`, `O_WRONLY`, `O_RDWR` — open for reading/writing/both,
  - `O_CREAT` — create the queue; this requires two additional arguments:  
    `mode_t mode`, which specifies access permissions, and  
    `mq_attr* attr`, a structure with 4 fields:
    - `mq_flags`, a bitmask (POSIX only defines the presence/absence of `O_NONBLOCK`, others are implementation-dependent),
    - `mq_maxmsg` — maximum number of messages in the queue,
    - `mq_msgsize` — maximum message size,
    - `mq_curmsgs` — current number of messages in the queue (ignored when creating the queue).  
      
    You can also pass `NULL` as `attr`, and the queue will be created with default settings.
  - `O_EXCL` — used only with `O_CREAT`, it causes an error if a queue with the given name already exists,
  - `O_NONBLOCK` — sets the queue to non-blocking mode.

The function returns a message queue descriptor (`mqd_t`) or `(mqd_t)-1` on error.  
Unused descriptors should be closed using `mq_close` (`man 3p mq_close`).

You can remove a queue by name using `mq_unlink`. Be cautious when attempting to remove a queue that is still open in another process. As noted in `man 3p mq_unlink`, unlinking only frees the name (e.g., so it can be reused). The queue is actually removed only after all associated descriptors are closed. Depending on the implementation, `mq_unlink` may block until other processes close their descriptors. On Linux (`man 3 mq_unlink`), however, it returns immediately.  
Even in that case, a newly created queue with the same name will be a different queue.]]></text>
    </section>
    <section id="queue-attributes" title="Queue Attributes" level="3" order="3" parent="posix-queue-operations">
      <text><![CDATA[### Queue Attributes

Queues store their attributes in a `mq_attr` structure, discussed in the previous section (see `mq_open`, `O_CREAT` flag). You can retrieve queue attributes using `mq_getattr` (`man 3p mq_getattr`):

```
int mq_getattr(mqd_t mqdes, struct mq_attr *mqstat);
```

- `mqdes` is the descriptor of the open queue,
- `mqstat` is a pointer to an already allocated `struct mq_attr` variable, whose fields will be filled by the function.

To set queue attributes, use `mq_setattr` (`man 3p mq_setattr`):

```
int mq_setattr(mqd_t mqdes, const struct mq_attr *restrict mqstat, struct mq_attr *restrict omqstat);
```

- `mqdes` is the descriptor of the open queue,
- `mqstat` points to the `mq_attr` structure with attributes you want to set,
- `omqstat` points to an already allocated `mq_attr` variable; its fields will be filled with values **before** the call to `mq_setattr`. You may also pass `NULL`.

**IMPORTANT**: The `mq_setattr` function only changes the `mq_flags` field. The other fields (`mq_maxmsg`, `mq_msgsize`, and `mq_curmsgs`) are immutable (set only during queue creation) and ignored by `mq_setattr`.  
In practice, according to POSIX, the only changeable setting is the blocking/non-blocking I/O mode.

Both functions return `0` on success or `-1` on failure.]]></text>
    </section>
    <section id="sending-and-receiving-messages" title="Sending and Receiving Messages" level="3" order="4" parent="posix-queue-operations">
      <text><![CDATA[### Sending and Receiving Messages

So far, we have used `read` and `write` to handle I/O with files and pipes. Message queues, however, use their own functions: `mq_send` and `mq_receive` (`man 3p mq_send`, `man 3p mq_receive`). Let’s look at their definitions:

```
int mq_send(mqd_t mqdes, const char *msg_ptr, size_t msg_len, unsigned msg_prio);
ssize_t mq_receive(mqd_t mqdes, char *msg_ptr, size_t msg_len, unsigned *msg_prio);
```

- `mqdes` is the queue descriptor,
- `msg_ptr` is a pointer to the message buffer (for sending) or the buffer to be filled (for receiving),
- `msg_len` is the size of the `msg_ptr` buffer in bytes (must not exceed `mq_msgsize` when sending; must be ≥ `mq_msgsize` when receiving),
- `msg_prio`:
  - for `mq_send`, this is the message's priority (must be less than `MQ_PRIO_MAX` — POSIX guarantees `MQ_PRIO_MAX >= 32`),
  - for `mq_receive`, it is a pointer to a variable where the message priority will be stored (can be `NULL` if the priority is not needed).

If you try to read from an empty queue or write to a full queue in non-blocking mode, the functions return `-1` and set `errno` to `EAGAIN`.  
In blocking mode, the functions will wait until the operation becomes possible.

If `mq_send` succeeds, it returns `0`.  
`mq_receive` returns the size (in bytes) of the received message.

In blocking mode, we can also set a maximum wait time for a message or for sending it. For this, use `mq_timedsend` and `mq_timedreceive`:

```
int mq_timedsend(mqd_t mqdes, const char *msg_ptr, size_t msg_len, unsigned msg_prio, const struct timespec *abstime);
ssize_t mq_timedreceive(mqd_t mqdes, char *restrict msg_ptr, size_t msg_len, unsigned *restrict msg_prio, const struct timespec *restrict abstime);
```

These functions add the `abstime` argument, a pointer to a `timespec` struct (from `<time.h>`), which defines an **absolute** timeout after which the function will stop waiting (to receive a message or to send one).  
They behave like their non-timed counterparts, except that if the timeout is exceeded, they return `-1` and set `errno` to `ETIMEDOUT`.

**IMPORTANT**: As mentioned, `abstime` is an absolute timeout. To use it correctly, you must first retrieve the current time using, for example, `clock_gettime` with `CLOCK_REALTIME` (`man 3p clock_getres`), then add the desired wait time (e.g., add `1` to `tv_sec` for a 1-second wait).

When the queue is in non-blocking mode, `mq_timed(send|receive)` behaves exactly the same as `mq_(send|receive)`.]]></text>
    </section>
    <section id="notifications" title="Notifications" level="3" order="5" parent="posix-queue-operations">
      <text><![CDATA[### Notifications

POSIX provides the ability to asynchronously notify processes about incoming messages to an empty queue.  
To register a process for notification, use the `mq_notify` function (`man 3p mq_notify`):

```
int mq_notify(mqd_t mqdes, const struct sigevent *notification);
```

- `mqdes` is the descriptor of the open queue,  
- `notification` is a pointer to a `sigevent` structure (header `<signal.h>`) that specifies how the notification should be delivered.

The `sigevent` structure should be filled out depending on the notification type (`man 7 sigevent`):

- For signal-based notification (only *realtime* signals):
  - set `sigev_notify` to `SIGEV_SIGNAL`,
  - set `sigev_signo` to the signal you want to receive (e.g., `SIGRTMIN`),
  - set `sigev_value.sival_int` or `sigev_value.sival_ptr` to the value you want to receive in the signal handler argument (an integer or a memory address, respectively).

  Additionally:
  - when setting the signal handler using the `sigaction` function, the `sigaction` structure passed to the function must have the `sa_flags` field set to `SA_SIGINFO` (see the `sethandler` function from the solution in Task 1),
  - the values passed in `sigev_value` are delivered to the handler in the `si_value` field of the `siginfo_t` structure (see the `mq_handler` function in the task solution).

- For thread-based notification:
  - set `sigev_notify` to `SIGEV_THREAD`,
  - set `sigev_notify_function` to a pointer to a function that takes a `union sigval` (the type of `sigev_value`) and returns `void`,
  - set `sigev_value.sival_int` or `sigev_value.sival_ptr` to the value you want to pass to the above function (integer or memory address),
  - optionally set thread parameters in `sigev_notify_attributes`.

To unregister a process from notifications, call `mq_notify` with `notification` set to `NULL`.  
Passing `NULL` when no process is registered will return `-1` and set `errno` to `EINVAL`.

**IMPORTANT**:
- Notifications are **one-time only**: after a notification is delivered, no further notifications are expected. To receive more, you must call `mq_notify` again.
- Notifications are only triggered when the queue transitions from **empty to non-empty**.
- Only one process can be registered for notification. Otherwise, `mq_notify` returns `-1` and sets `errno` to `EBUSY`.

Refer to the signal-based notification implementation in Task 1.  
Thread-based notification is demonstrated in the solution for Task 2.]]></text>
    </section>
    <section id="notes" title="Notes" level="3" order="6" parent="posix-queue-operations">
      <text><![CDATA[### Notes

1. You must link the `librt` library when compiling programs that use POSIX message queues.
2. If you open an existing queue, it may already contain data. You should not assume it is empty. To ensure a clean state, delete the queue before recreating it.]]></text>
    </section>
    <section id="tasks-with-solutions" title="Tasks with Solutions" level="2" order="7" parent="tutorial-5-5-posix-message-queues">
      <text><![CDATA[## Tasks with Solutions]]></text>
    </section>
    <section id="task-1-signal-based-notification" title="Task 1: Signal-Based Notification" level="3" order="8" parent="tasks-with-solutions">
      <text><![CDATA[### Task 1: Signal-Based Notification

Write a program that simulates a simple version of *bingo*. The parent process randomly draws numbers, and the players are its child processes. Communication between them takes place via POSIX message queues.

The parent process creates `n` child processes (`0 < n < 100`, where `n` is a program argument) and two message queues.  
The first queue, `pout`, is used to send randomly drawn numbers from the range `[0,9]` to the children once per second.  
The second queue, `pin`, is used to receive messages from the children when they win or complete the game.

Each child process randomly selects its **winning number** `E` (in `[0,9]`) and a number `N` representing how many numbers it will read from the queue (also from `[0,9]`). Then, in a loop, the child tries to read numbers from the `pout` queue — a sent number can only be received by one process, not all of them simultaneously.

Each child compares the received number with its `E`. If they match, the child sends this number to the parent via the `pin` queue and exits.  
If the child reads `N` numbers without a match, it sends its own process number (from `[1, n]`) to the parent via `pin` before exiting.

The parent process must asynchronously receive messages from the `pin` queue and print the appropriate output, while still sending numbers every second.  
When all child processes terminate, the parent also ends execution and removes both queues.

**NOTE**: For this task, message size in the queue should be limited to **1 byte**!]]></text>
    </section>
    <section id="task-solution" title="Task Solution" level="4" order="9" parent="task-1-signal-based-notification">
      <text><![CDATA[#### Task Solution

New manpages:

- `man 7 mq_overview`
- `man 3p mq_open`
- `man 3p mq_close`
- `man 3p mq_unlink`
- `man 3p mq_getattr`
- `man 3p mq_setattr`
- `man 3p mq_send`
- `man 3p mq_receive`
- `man 3p mq_notify`
- `man 7 sigevent`


```c
#define _GNU_SOURCE
#include <errno.h>
#include <mqueue.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/wait.h>
#include <time.h>
#include <unistd.h>

#define LIFE_SPAN 10
#define MAX_NUM 10

#define ERR(source) \
    (fprintf(stderr, "%s:%d\n", __FILE__, __LINE__), perror(source), kill(0, SIGKILL), exit(EXIT_FAILURE))

volatile sig_atomic_t children_left = 0;

void sethandler(void (*f)(int, siginfo_t *, void *), int sigNo)
{
    struct sigaction act;
    memset(&act, 0, sizeof(struct sigaction));
    act.sa_sigaction = f;
    act.sa_flags = SA_SIGINFO;
    if (-1 == sigaction(sigNo, &act, NULL))
        ERR("sigaction");
}

void mq_handler(int sig, siginfo_t *info, void *p)
{
    mqd_t *pin;
    uint8_t ni;
    unsigned msg_prio;

    pin = (mqd_t *)info->si_value.sival_ptr;

    static struct sigevent notif;
    notif.sigev_notify = SIGEV_SIGNAL;
    notif.sigev_signo = SIGRTMIN;
    notif.sigev_value.sival_ptr = pin;
    if (mq_notify(*pin, &notif) < 0)
        ERR("mq_notify");

    for (;;)
    {
        if (mq_receive(*pin, (char *)&ni, 1, &msg_prio) < 1)
        {
            if (errno == EAGAIN)
                break;
            else
                ERR("mq_receive");
        }
        if (0 == msg_prio)
            printf("MQ: got timeout from %d.\n", ni);
        else
            printf("MQ: %d is a bingo number!\n", ni);
    }
}

void sigchld_handler(int sig, siginfo_t *s, void *p)
{
    pid_t pid;
    for (;;)
    {
        pid = waitpid(0, NULL, WNOHANG);
        if (pid == 0)
            return;
        if (pid <= 0)
        {
            if (errno == ECHILD)
                return;
            ERR("waitpid");
        }
        children_left--;
    }
}

void child_work(int n, mqd_t pin, mqd_t pout)
{
    srand(getpid());
    uint8_t my_bingo = (uint8_t)(rand() % MAX_NUM);
    uint8_t ni;
    for (int life = rand() % LIFE_SPAN + 1; life > 0; life--)
    {
        if (TEMP_FAILURE_RETRY(mq_receive(pout, (char *)&ni, 1, NULL)) < 1)
            ERR("mq_receive");
        printf("[%d] Received %d\n", getpid(), ni);
        if (my_bingo == ni)
        {
            if (TEMP_FAILURE_RETRY(mq_send(pin, (const char *)&my_bingo, 1, 1)))
                ERR("mq_send");
            return;
        }
    }
    if (TEMP_FAILURE_RETRY(mq_send(pin, (const char *)&n, 1, 0)))
        ERR("mq_send");
}

void parent_work(mqd_t pout)
{
    srand(getpid());
    while (children_left)
    {
        uint8_t ni = (uint8_t)(rand() % MAX_NUM);
        if (TEMP_FAILURE_RETRY(mq_send(pout, (const char *)&ni, 1, 0)))
            ERR("mq_send");
        sleep(1);
    }
    printf("[PARENT] Terminates \n");
}

void create_children(int n, mqd_t pin, mqd_t pout)
{
    while (n > 0)
    {
        switch (fork())
        {
            case 0:
                child_work(n, pin, pout);
                exit(EXIT_SUCCESS);
            case -1:
                ERR("fork");
        }
        children_left++;
        n--;
    }
}

void usage(char *name)
{
    fprintf(stderr, "USAGE: %s n k p l\n", name);
    fprintf(stderr, "100 > n > 0 - number of children\n");
    exit(EXIT_FAILURE);
}

int main(int argc, char **argv)
{
    int n;
    if (argc != 2)
        usage(argv[0]);
    n = atoi(argv[1]);
    if (n <= 0 || n >= 100)
        usage(argv[0]);

    mqd_t pin, pout;
    struct mq_attr attr = {};
    attr.mq_maxmsg = 10;
    attr.mq_msgsize = 1;
    if ((pin = TEMP_FAILURE_RETRY(mq_open("/bingo_in", O_RDWR | O_NONBLOCK | O_CREAT, 0600, &attr))) == (mqd_t)-1)
        ERR("mq_open in");
    if ((pout = TEMP_FAILURE_RETRY(mq_open("/bingo_out", O_RDWR | O_CREAT, 0600, &attr))) == (mqd_t)-1)
        ERR("mq_open out");

    sethandler(sigchld_handler, SIGCHLD);
    sethandler(mq_handler, SIGRTMIN);
    create_children(n, pin, pout);

    static struct sigevent noti;
    noti.sigev_notify = SIGEV_SIGNAL;
    noti.sigev_signo = SIGRTMIN;
    noti.sigev_value.sival_ptr = &pin;
    if (mq_notify(pin, &noti) < 0)
        ERR("mq_notify");

    parent_work(pout);

    mq_close(pin);
    mq_close(pout);
    if (mq_unlink("/bingo_in"))
        ERR("mq unlink");
    if (mq_unlink("/bingo_out"))
        ERR("mq unlink");
    return EXIT_SUCCESS;
}
```]]></text>
      <code ref="src/l5_5-1.c" lang="c"/>
    </section>
    <section id="notes-and-questions" title="Notes and Questions" level="4" order="10" parent="task-1-signal-based-notification">
      <text><![CDATA[#### Notes and Questions

- Pay attention to the use of the pointer sent with the signal. The signal handler function prototype includes an additional `siginfo_t*` parameter, and the `SA_SIGINFO` flag is used when installing the handler to enable pointer passing. Note that we are not sending this signal manually — we receive it as a notification from the queue. The `kill` function cannot send a pointer; this can only be done using `sigqueue`.

- Numbers are passed using the `uint8_t` type (`stdint.h`), which is a one-byte unsigned integer ranging from 0 to 255. Fixed-size integer types (e.g., `int32_t`) are more portable than base types like `int`, which can vary in size across platforms.

- Since the program involves signal handling, protection from signal interruptions is required, e.g., using `TEMP_FAILURE_RETRY`. These macros were added throughout the code, although only the parent process's code is at risk of interruption, as it is the only one receiving notifications from the queue. Adding such safeguards does not degrade performance and makes the code more portable.

- One of the parent process’s calls lacks signal interruption protection — which one, and why is it not necessary there?  
  
**Answer:** The `mq_receive` call inside the signal handler function. We don't expect the handler itself to be interrupted by the signal it's handling, because that signal is blocked during its own handling by default.


- How are child processes counted?  
  
**Answer:** By counting successful `waitpid` calls in the `SIGCHLD` handler. Signals like `SIGCHLD` can coalesce, so we do not count the signals themselves. The counter is stored in a global variable.


- Why do we install/restart the notification before reading from the queue?  
  
**Answer:** If we read the queue first and install the notification afterward, a message could arrive in the queue in between — and because notifications are triggered only when a message appears in an *empty* queue, we would miss it. Installing the notification first guarantees that the queue is empty and a new message will generate a notification.


- Why is one of the queues set to non-blocking mode?  
  
**Answer:** The `pin` queue is non-blocking due to the above reason. If it blocked, the program could hang inside the asynchronous signal handler after reading the last message, which would be a serious error.


- How are message priorities used, and how does that relate to the 1-byte message size limit?  
  
**Answer:** Priorities are used not for ordering but to indicate the type of message. Because we’re limited to 1-byte messages, it’s hard to encode anything beyond a single number. Whether it’s a winning message or a game-over notice is indicated via the priority.


- A significant part of the program logic is implemented inside the signal handler, which is acceptable because there's no dependency between the number-drawing code and the notification-handling code. However, to make it more challenging, try moving all message-handling logic into the main parent process code (i.e., outside the asynchronous signal handler).]]></text>
    </section>
    <section id="task-2-thread-based-notification" title="Task 2: Thread-based Notification" level="3" order="11" parent="tasks-with-solutions">
      <text><![CDATA[### Task 2: Thread-based Notification

Write a program simulating conversations on a Roman forum.  
Define a constant `CHILD_COUNT`.  
The parent process creates `CHILD_COUNT` queues, opens them in non-blocking mode, and spawns `CHILD_COUNT` child processes.  
Each child receives a randomly generated name.  
Each child acts as a citizen on the forum.  
Citizens randomly send each other messages containing their names, and upon receiving messages, they print them prefixed with their own name.  
After each message is sent, they sleep for a random amount of time.  
After sending `ROUNDS` messages, they exit.  
Each citizen waits for messages using thread-based notification.]]></text>
    </section>
    <section id="solution" title="Solution" level="4" order="12" parent="task-2-thread-based-notification">
      <text><![CDATA[#### Solution


```c
#include <errno.h>
#include <fcntl.h>
#include <mqueue.h>
#include <pthread.h>
#include <signal.h>
#include <stdio.h>
#include <stdlib.h>
#include <sys/wait.h>
#include <unistd.h>

#define CHILD_COUNT 4

#define QUEUE_NAME_MAX_LEN 32
#define CHILD_NAME_MAX_LEN 32

#define MSG_SIZE 64
#define MAX_MSG_COUNT 4

#define ROUNDS 5

#define ERR(source) \
    (fprintf(stderr, "%s:%d\n", __FILE__, __LINE__), perror(source), kill(0, SIGKILL), exit(EXIT_FAILURE))

mqd_t create_queue(char* name)
{
    struct mq_attr attr = {};
    attr.mq_maxmsg = MAX_MSG_COUNT;
    attr.mq_msgsize = MSG_SIZE;

    mqd_t res = mq_open(name, O_RDWR | O_NONBLOCK | O_CREAT, 0600, &attr);
    if (res == -1)
        ERR("mq_open");

    return res;
}

typedef struct
{
    char* name;
    mqd_t queue;
} ChildData;

void handle_messages(union sigval data);

void register_notification(ChildData* data)
{
    struct sigevent notification = {};
    notification.sigev_value.sival_ptr = data;
    notification.sigev_notify = SIGEV_THREAD;
    notification.sigev_notify_function = handle_messages;

    int res = mq_notify(data->queue, &notification);
    if (res == -1)
        ERR("mq_notify");
}

void handle_messages(union sigval data)
{
    ChildData* child_data = data.sival_ptr;
    char message[MSG_SIZE];

    register_notification(child_data);

    for (;;)
    {
        int res = mq_receive(child_data->queue, message, MSG_SIZE, NULL);
        if (res != -1)
            printf("%s: Accepi \"%s\"\n", child_data->name, message);
        else
        {
            if (errno == EAGAIN)
                break;
            else
                ERR("mq_receive");
        }
    }
}

void close_queues(mqd_t* queues)
{
    for (int i = 0; i < CHILD_COUNT; ++i)
    {
        mq_close(queues[i]);
    }
}

void unlink_queues(char queue_names[CHILD_COUNT][CHILD_NAME_MAX_LEN])
{
    for (int i = 0; i < CHILD_COUNT; ++i)
    {
        int result = mq_unlink(queue_names[i]);
        // The case when the queue does not exist already
        // is not treated as an error
        if (result == -1 && errno != ENOENT)
            ERR("mq_close");
    }
}

void send_message(mqd_t queue, char* content)
{
    int result = mq_send(queue, content, MSG_SIZE, 0);
    if (result == -1)
        ERR("mq_send");
}

void child_function(char* name, mqd_t* queues, int i)
{
    printf("%s: A PID %d incipiens.\n", name, getpid());
    srand(getpid());

    ChildData child_data = {};
    child_data.name = name;
    child_data.queue = queues[i];

    union sigval data;
    data.sival_ptr = &child_data;
    handle_messages(data);

    for (int i = 0; i < ROUNDS; ++i)
    {
        int receiver = rand() % CHILD_COUNT;
        char message[MSG_SIZE];
        switch (rand() % 3)
        {
            case 0:
                snprintf(message, MSG_SIZE, "%s: Salve %d!", name, receiver);
                break;
            case 1:
                snprintf(message, MSG_SIZE, "%s: Visne garum emere, %d?", name, receiver);
                break;
            case 2:
                snprintf(message, MSG_SIZE, "%s: Fuistine hodie in thermis, %d?", name, receiver);
                break;
        }

        send_message(queues[receiver], message);

        int sleep_time = rand() % 3 + 1;
        sleep(sleep_time);
    }

    printf("%s: Disceo.\n", name);

    exit(EXIT_SUCCESS);
}

void spawn_child(char* name, mqd_t* queues, int i)
{
    switch (fork())
    {
        case 0:
            child_function(name, queues, i);
        case -1:
            ERR("fork");
    }
}

int main(void)
{
    mqd_t queues[CHILD_COUNT];
    char names[CHILD_COUNT][CHILD_NAME_MAX_LEN];
    char queue_names[CHILD_COUNT][CHILD_NAME_MAX_LEN];

    for (int i = 0; i < CHILD_COUNT; ++i)
    {
        snprintf(queue_names[i], QUEUE_NAME_MAX_LEN, "/child_%d", i);
        queues[i] = create_queue(queue_names[i]);

        snprintf(names[i], CHILD_NAME_MAX_LEN, "Persona %d", i);
    }

    for (int i = 0; i < CHILD_COUNT; ++i)
    {
        spawn_child(names[i], queues, i);
    }

    close_queues(queues);

    while (wait(NULL) > 0)
        ;

    printf("Parens: Disceo.");

    unlink_queues(queue_names);

    return EXIT_SUCCESS;
}
```]]></text>
      <code ref="src/l5_5-2.c" lang="c"/>
    </section>
    <section id="notes-and-questions-2" title="Notes and Questions" level="4" order="13" parent="task-2-thread-based-notification">
      <text><![CDATA[#### Notes and Questions

- Why do we break the `mq_receive` loop on `EAGAIN`?  
  
**Answer:** This error means the queue is empty and there’s nothing more to read.


- Why does `child_function` call `handle_messages` and not just `register_notification`?  
  
**Answer:** Because if messages are already in the queue from other children, no notification will be triggered. We must read those messages in the main thread.


- The parent process closes all queue descriptors immediately after creating the children. Why doesn’t this cause problems for the children?  
  
**Answer:** Because children inherit a copy of the parent's data, including file descriptors. Closing them in the parent does not affect the children’s copies.


- Why don’t the child processes close the queues at the end?  
  
**Answer:** Closing a queue removes its notification, meaning no new handler threads will start. However, if a thread is already running while the queue is closed, it may attempt `mq_notify` or `mq_receive` on an invalid descriptor. Therefore, queues must remain open until the program ends. The OS will close them when the program exits, so it’s not an error.


- Could we just wait for the notification handler thread to finish before closing the queues?  
  
**Answer:** No. We don’t have the thread’s TID. Even if it were shared, we wouldn’t know if the thread still exists when closing queues. Also, the thread might be in `detached` mode — such threads can’t be joined.


- Could we use a synchronization structure to prevent this issue?  
  
**Answer:** Unfortunately, no. If we used one, we’d need to destroy it at the end of the program. But the thread might still be running and try to access the destroyed structure. In this case, there’s no perfect solution. Since closing the queue means the program is ending anyway, if threads were writing to a file, we could use a condition variable to ensure they finish — but in this case, it’s unnecessary.


- Can we move the `exit(EXIT_SUCCESS)` call from `child_function` to `spawn_child`, right after calling `child_function`?  
  
**Answer:** No, for the same reason we don’t close queues. The `child_data` would be out of scope after `child_function`, but it’s still needed by the handler thread. Leaving `exit(EXIT_SUCCESS)` inside `child_function` ensures it stays valid.]]></text>
    </section>
    <section id="example-exercises" title="Example Exercises" level="2" order="14" parent="tutorial-5-5-posix-message-queues">
      <text><![CDATA[## Example Exercises

Complete the sample exercises. You will have more time and starter code during the lab session, but completing the tasks below on your own means you are well prepared.

- [Exercise 1](https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example1/) ~90 minutes  
- [Exercise 2](https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example2/) ~120 minutes  
- [Exercise 3](https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example3/) ~120 minutes  
- [Exercise 4](https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example4/) ~150 minutes]]></text>
    </section>
    <section id="source-code-from-the-tutorial" title="Source Code from the Tutorial" level="2" order="15" parent="tutorial-5-5-posix-message-queues">
      <text><![CDATA[## Source Code from the Tutorial

- [l5_5-1.c](l5_5-1.c)
- [l5_5-2.c](l5_5-2.c)]]></text>
    </section>
  </tutorial>
  <tasks>
    <task id="example1" title="Task on POSIX message queues" source_url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example1/">
      <statement><![CDATA[## Goal

Write 2 programs, client and server, that communicate via POSIX message queues.

The server program creates 3 message queues with names `PID_s`, `PID_d` and `PID_m` where `PID` is the server process ID. The server program prints the names of all queues to stdout.

The server waits for data in any of its queues, receiving a client PID and two integers. Then, computes the result: sum for `PID_s` queue, division for `PID_d`, and modulo for `PID_m`. The result is sent to the corresponding client queue (see below). The server should not terminate if the client queue is unavailable. After receiving `SIGINT` the server destroys its queues and terminates.

The client process is called with a single argument: the name of the server queue. The client creates its own queue (the name is the client's PID) and continues to read lines containing two integers until it encounters `EOF`. After receiving a line the client sends a message containing its PID and the two integers to the server queue and waits for a reply. The reply is displayed on stdout. If the reply does not arrive within 100 ms, the program terminates. While terminating, the program destroys its queue.]]></statement>
      <stages>
        <stage n="1"><![CDATA[The server creates its queues and displays the names of the queues. After 1 second, it destroys those queues and terminates. The client process creates its own queue, waits for 1 second, destroys its queue, and terminates.]]></stage>
        <stage n="2"><![CDATA[Server reads the first message from `PID_s` queue. Sends the first answer back to the client. Ignores all errors. The client reads 2 integers from stdin and sends a single message to the server. It waits for the result and displays it.]]></stage>
        <stage n="3"><![CDATA[The server handles all queues and calculates the proper results. Terminates at `SIGINT`. The client sends the messages until `EOF` is read or reply timeout occurs.]]></stage>
        <stage n="4"><![CDATA[The queues should be removed on program termination. Full error handling.]]></stage>
      </stages>
      <attachments/>
      <notes><![CDATA[]]></notes>
    </task>
    <task id="example2" title="Task on POSIX message queues" source_url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example2/">
      <statement><![CDATA[## Employees

Create a task distribution system for employee processes using POSIX message queues.  
The system will simulate work by adding two random floating-point numbers (range 0-100.0) and putting the process to sleep for a random duration (500 ms - 2000 ms).  

The main process (server) adds new tasks to the queue at random intervals (`T₁` - `T₂` ms, where `100 <= T₁ < T₂ <= 5000`).  
Each task consists of a pair of random floating-point numbers from the range `[0.0, 100.0]`.  

At the start, child processes (N workers, where `2 <= N <= 20`) are created and register themselves in the task queue named `"task_queue_{server_pid}"`.  
Workers, while waiting for tasks, retrieve available tasks from the queue when they are idle.  
Each worker returns the results through its own queue named `"result_queue_{server_pid}_{worker_id}"`.  

**Note:** The program should support multiple instances without queue name collisions!  

## Example Program Execution:

```sh
./sop-qwp 3 500 2000
Server is starting...
[5831] Worker ready!
[5832] Worker ready!
New task queued: [84.65, 17.80]
[5831] Received task [84.65, 17.80]
[5833] Worker ready!
[5831] Result sent [102.46]
Result from worker 0: 102.46
New task queued: [25.87, 10.67]
[5832] Received task [25.87, 10.67]
New task queued: [84.04, 7.21]
[5833] Received task [84.04, 7.21]
[5832] Result sent [36.55]
Result from worker 1: 36.55
[5833] Result sent [91.25]
Result from worker 2: 91.25
New task queued: [83.46, 35.65]
[5831] Received task [83.46, 35.65]
New task queued: [38.44, 27.78]
[5832] Received task [38.44, 27.78]
[5831] Result sent [119.12]
Result from worker 0: 119.12
^CClosing server...
[5833] Exits
[5831] Exits
[5832] Result sent [66.23]
[5832] Exits
Result from worker 1: 66.23
All child processes have finished.
```]]></statement>
      <stages>
        <stage n="1"><![CDATA[The main process creates N workers, who after performing a random sleep (500 ms - 2000 ms) finish their work.  
   At the start, the server prints `"Server is starting..."` and after all workers have finished, it prints `"All child processes have finished."`  
   Workers print `"[{worker_pid}] Worker ready!"` at startup and `"[{worker_pid}] Exits!"` upon finishing.]]></stage>
        <stage n="2"><![CDATA[The server creates `5 * N` tasks at random time intervals (`T₁` to `T₂` ms) and adds them to the queue.  
   The server informs when a task is added: `"New task queued: [{v1}, {v2}]"` or if the queue is full: `"Queue is full!"`.   
   Workers, upon receiving a task, print: `"[{worker_pid}] Received task [{v1}, {v2}]"`, sleep randomly (500-2000ms),  
   print the result as `"[{worker_pid}] Result [{result_value}]"`, and wait for the next task. They stop working after completing 5 tasks.]]></stage>
        <stage n="3"><![CDATA[Workers send results to the server through their individual queues and inform about it on standard output:  
   `"[{worker_pid}] Result sent [{value}]"`  
   The server receives results and prints: `"Result from worker {worker_pid}: {value}"`]]></stage>
        <stage n="4"><![CDATA[The main process continues to create tasks until it receives a `SIGINT` signal, after which it informs workers to stop (via the queue).  
   The server waits for workers to finish their current tasks before terminating.  
   Workers finish after receiving the stop notification from the server (they complete only the tasks they started—tasks still in the queue are ignored).  
   All resources are properly released.]]></stage>
      </stages>
      <attachments/>
      <notes><![CDATA[]]></notes>
    </task>
    <task id="example3" title="Task on POSIX message queues" source_url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example3/">
      <statement><![CDATA[## Chinese Whispers  

> The Chinese whispers game, also known as "Telephone," involves players forming a line and passing a message from one person to the next, usually resulting in a humorous distortion of the original message by the time it reaches the last player.

Write a program that simulates the game of Chinese whispers using POSIX message queues.  

The program launches with three parameters `P`, `T₁`, `T₂`, where `0 ≤ P ≤ 100` and `100 ≤ T₁ < T₂ ≤ 6000`.  
Upon launching, the names of children participating in the game are fed (on new lines) into the standard input.  

Entering the command `start "Very long sentence to pass"` starts the game. The coordinator (main process) passes the given sentence in its unchanged form to the first child (in the order they joined the game). Each word is sent in a separate message.  

After the game round is finished, the coordinator displays the result, and all processes are terminated.  

A message queue can store only 2 words (messages) at once.  

It can be assumed that the maximum length of a line of standard input is 255 characters.  

**Note:** The program should handle multiple instances without queue name collisions and program operation conflicts!  

### Sample Input (`example_input.txt`):  

```plaintext
Anna
Frank
Simon
Katie
Tom
Matt
start "Operating Systems 1 and 2 are my favorite courses during my studies; I have learned a lot of fun stuff here!"
```

### Expected Output:

```plaintext
./sop-cwg-stage4 2 250 500 < example_input.txt
Chinese Whispers Game
Provide child name or type start to begin the game!
[29406] Anna has joined the game!
[29407] Frank has joined the game!
[29408] Simon has joined the game!
[29409] Katie has joined the game!
[29410] Tom has joined the game!
[29411] Matt has joined the game!
Game has started with 6 childrens!
Passing "Operating Systems 1 and 2 are my favorite courses during my studies; I have learned a lot of fun stuff here!" to the first child!
Passing word "Operating"
Passing word "Systems"
Passing word "1"
[29406] Anna got message: Operating
Passing word "and"
[29406] Anna got message: Systems
Passing word "2"
[29407] Frank got message: Operating
Passing word "are"
[29406] Anna got message: 1
[29407] Frank got message: Systems
[29408] Simon got message: Operating
Passing word "my"
[29407] Frank got message: 1
[29406] Anna got message: and
[29408] Simon got message: Systems
[29409] Katie got message: Operating
[29407] Frank got message: and
[29406] Anna got message: 2
Passing word "favorite"
[29410] Tom got message: Operating
[29408] Simon got message: 1
[29409] Katie got message: Systems
[29411] Matt got message: Operating
...
[29408] Simon has left the game!
[29411] Matt got message: fun
[29409] Katie got message: heraa
[29410] Tom got message: stucf
[29411] Matt got message: stucf
[29410] Tom got message: hewaa
[29411] Matt got message: hewaa
[29409] Katie has left the game!
[29410] Tom has left the game!
[29411] Matt has left the game!
Final message    : Operating Systems 1 and 2 lre my favorite courbes duhing my studies; I zave learned a lot ou fun stuuf hewaa
Original message : Operating Systems 1 and 2 are my favorite courses during my studies; I have learned a lot of fun stuff here!
Game has ended!
```]]></statement>
      <stages>
        <stage n="1"><![CDATA[The main process (game coordinator) correctly creates new processes based on the given names of the children and waits for their termination.]]></stage>
        <stage n="2"><![CDATA[There is no predetermined limit (apart from system resources) on the number of children participating in the game.]]></stage>
        <stage n="3"><![CDATA[At a minimum, to start the game, there must be at least one child. Each child knows only their name and the child's PID from which they listen. In the case of the first child, it is the PID of the coordinator.]]></stage>
        <stage n="4"><![CDATA[A child displays the message `[{PID}] {name} has joined the game!`, waits a random time between `T₁` and `T₂`, and exits with the message `[{PID}] {name} has left the game!`.]]></stage>
        <stage n="5"><![CDATA[A child, upon joining the game, creates a new queue named `"sop_cwg_{PID}"`.]]></stage>
        <stage n="6"><![CDATA[The child knows only the PID of the preceding child - this tells them which queue to receive messages from.]]></stage>
        <stage n="7"><![CDATA[Typing `start {message_to_pass}` starts sending word-by-word the given message to the first child.]]></stage>
        <stage n="8"><![CDATA[Only the first child receives the message from the coordinator for this stage.]]></stage>
        <stage n="9"><![CDATA[Upon receiving a word, it prints it to standard output `[{PID}] {name} got the message: '{word}'`.]]></stage>
        <stage n="10"><![CDATA[Words are transmitted between the children and then returned back to the coordinator.]]></stage>
        <stage n="11"><![CDATA[For each letter of the word, there is a `P%` chance that the letter will be changed to some random Latin letter.]]></stage>
        <stage n="12"><![CDATA[The coordinator prints the received message on standard output. All used resources are correctly released.]]></stage>
        <stage n="13"><![CDATA[After receiving the `SIGINT` signal, all children quit the game.]]></stage>
        <stage n="14"><![CDATA[The coordinator displays the part of the message he has already received and exits.]]></stage>
        <stage n="15"><![CDATA[You can use a timeout for messages to avoid a deadlock in this stage.  

---]]></stage>
      </stages>
      <attachments/>
      <notes><![CDATA[]]></notes>
    </task>
    <task id="example4" title="Task on POSIX message queues" source_url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example4/">
      <statement><![CDATA[## Chat

Write a server and a client for a simple chat using POSIX queues.  
When the server starts, it opens a queue named `"chat_{server_name}"` where `{server_name}` is the only server argument. This queue is used for incoming communication from clients to the server.  

The client starts with two arguments: `{server_name}` and `{client_name}`. It connects to the server using the server queue, opens its own queue `"chat_{client_name}"` for reading messages from the server, and sends all typed messages from standard input to the server.  

There are three types of messages (use message priority to distinguish the type):  
- **0** - connection  
- **1** - disconnection  
- **2** - regular messages  

The connection message sent to the server from the client contains its name. The disconnection message is an empty 1-byte message signaling client or server termination. Regular messages from clients contain raw messages sent as C-strings without any metadata. The server sends back to all clients messages formatted as: `"[{sender_name}] {message}"`.  

The server can also send its own messages typed from the standard input; in that case, the sender name is `SERVER`.  

Use `mq_notify` for obtaining messages and information about the sender `PID`.  

---]]></statement>
      <stages>
        <stage n="1"><![CDATA[The server opens queue `"chat_{server_name}"`, reads from it, and displays in a loop a read message on the standard output as: `"[{msg_prio}] {msg_content}"`.  
   The client opens `"chat_{server_name}"` and sends its name there with the proper message priority.]]></stage>
        <stage n="2"><![CDATA[The client creates its queue `"chat_{client_name}"` at the start.  
   The server opens a client's queue after receiving the client's name (sent as a raw C-string with priority 0) and stores information about connected clients (max 8 clients connected at once).  
   On a client connection, the server displays: `"Client `{client_name}` has connected!"` on the standard output.]]></stage>
        <stage n="3"><![CDATA[A client sends lines from the standard input to the server.  
   The server receives data from clients using `mq_notify`. It displays them on the standard output formatted as: `"[{sender_name}] {message}"` and broadcasts them to all clients in the same format.  
   The server also supports typing messages, which are sent with `SERVER` as the sender name.  
   Clients receive messages from the server and display them as: `"[{client_name}] {message}"`.]]></stage>
        <stage n="4"><![CDATA[The server supports closing on `Ctrl-C`. It sends information about its closing using an "empty" message with the proper priority.  
   Upon receiving the server closing message, clients display: `"Server closed the connection"` and terminate.  
   Upon receiving the client closing message, the server displays: `"Client {client_name} disconnected!"`.  
   All resources are correctly released – queues are closed and removed by their creators.]]></stage>
      </stages>
      <attachments/>
      <notes><![CDATA[]]></notes>
    </task>
  </tasks>
  <sources>
    <file ref="src/l5_5-1.c" lang="c" source_url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/l5_5-1.c"/>
    <file ref="src/l5_5-2.c" lang="c" source_url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/l5_5-2.c"/>
  </sources>
  <references>
    <ref url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example1/" title="Exercise 1"/>
    <ref url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example2/" title="Exercise 2"/>
    <ref url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example3/" title="Exercise 3"/>
    <ref url="https://sop.mini.pw.edu.pl/en/sop2/lab/l5_5/example4/" title="Exercise 4"/>
  </references>
  <metadata/>
</lab>
